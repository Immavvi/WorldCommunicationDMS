# WCDMS V1 UI reference

`wcdms-ui-reference.png` is the owner-approved WCDMS V1 visual baseline. Frontend UI work must preserve this design unless the owner explicitly approves a future change. Business data shown in the image is illustrative and must never be hard-coded into the application.
