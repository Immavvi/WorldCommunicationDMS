import {useEffect,useState} from "react";import {Link} from "react-router-dom";import {getDashboard,type Dashboard} from "../api/reporting";import {useAuth} from "../auth/AuthContext";
const labels:Record<string,string>={active_projects:"Active Projects",active_loas:"Active LOAs",open_variations:"Open Variations",open_requirements:"Open Requirements",active_purchase_orders:"Active POs",overdue_deliveries:"Overdue Deliveries",verified_received_quantity:"Verified Received",dispatched_quantity:"Dispatched",assets_total:"Registered Assets",assets_available:"Available Assets",assets_installed:"Installed / In Service",assets_exception:"Asset Exceptions",critical_alerts:"Critical Alerts",high_alerts:"High Alerts",my_attention:"My Attention",unread_notifications:"Unread Notifications",pi_value:"PI Value",invoice_value:"Tax Invoice Value",received:"Collected",outstanding:"Outstanding",overdue_outstanding:"Overdue Outstanding",overdue_invoices:"Overdue Invoices",unallocated_receipts:"Unallocated Receipts"};
const money=new Set(["pi_value","invoice_value","received","outstanding","overdue_outstanding","unallocated_receipts"]);const indian=(value:string|number)=>`₹${Number(value).toLocaleString("en-IN",{minimumFractionDigits:2,maximumFractionDigits:2})}`;
export function DashboardPage(){const {token}=useAuth();const [data,setData]=useState<Dashboard|null>(null);useEffect(()=>{if(token)void getDashboard(token).then(setData);},[token]);if(!data)return <p>Loading dashboard…</p>;const cards=(values:Record<string,string|number>)=>Object.entries(values).map(([key,value])=><Link key={key} to={key.includes("alert")||key==="my_attention"?"/alerts":key.includes("asset")?"/reports?report=assets":key.includes("overdue")||key==="outstanding"?"/reports?report=receivables":"/reports"} className="rounded border p-4"><p className="text-sm text-slate-400">{labels[key]??key}</p><strong className="text-2xl">{money.has(key)?indian(value):String(value)}</strong></Link>);return <section className="space-y-8"><div><p className="text-sm tracking-[.2em] text-cyan-400">MANAGEMENT</p><h1 className="text-3xl font-semibold">Dashboard</h1></div><section><h2 className="text-xl">Operations</h2><div className="grid gap-3 md:grid-cols-4">{cards(data.operational)}</div></section>{data.financial&&<section><h2 className="text-xl">Finance & Collections</h2><div className="grid gap-3 md:grid-cols-4">{cards(data.financial)}</div></section>}</section>}
