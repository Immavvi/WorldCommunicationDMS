import { FormEvent, useCallback, useEffect, useState } from "react";
import { Link } from "react-router-dom";
import { listLoas, listProjects, type Loa, type Project } from "../api/contracts";
import { createInvoice, getInvoiceablePosition, listInvoices, type InvoiceablePosition, type TaxInvoice } from "../api/invoicing";
import { listMasterData, listTermsVersions, type MasterRecord, type TermsVersion } from "../api/masterData";
import { useAuth } from "../auth/AuthContext";

const resources = ["organizations", "gst-registrations", "bank-accounts", "payment-terms", "terms-condition-sets", "party-addresses", "railway-divisions", "railway-authorities", "railway-authority-addresses"];
const label = (record: MasterRecord) => String(record.data.name ?? record.data.label ?? record.data.legal_name ?? record.data.registered_name ?? record.data.bank_name ?? record.id);

export function TaxInvoicesPage() { const { token } = useAuth(); const [invoices, setInvoices] = useState<TaxInvoice[]>([]); const [projects, setProjects] = useState<Project[]>([]); const [loas, setLoas] = useState<Loa[]>([]); const [masters, setMasters] = useState<Record<string, MasterRecord[]>>({}); const [versions, setVersions] = useState<TermsVersion[]>([]); const [projectId, setProjectId] = useState(""); const [eligible, setEligible] = useState<InvoiceablePosition[]>([]); const project = projects.find((item) => item.id === projectId);
  const load = useCallback(async () => { if (!token) return; const [invoiceItems, projectItems, loaItems, ...lists] = await Promise.all([listInvoices(token), listProjects(token), listLoas(token), ...resources.map((resource) => listMasterData(token, resource))]); setInvoices(invoiceItems); setProjects(projectItems); setLoas(loaItems); const mapped = Object.fromEntries(resources.map((resource, index) => [resource, lists[index].items])); setMasters(mapped); const termsSet = mapped["terms-condition-sets"]?.[0]; setVersions(termsSet ? await listTermsVersions(token, termsSet.id) : []); }, [token]); useEffect(() => { void load(); }, [load]); useEffect(() => { if (token && projectId) void getInvoiceablePosition(token, projectId).then(setEligible); else setEligible([]); }, [token, projectId]);
  async function create(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!token || !project) return; const form = event.currentTarget; const data = new FormData(form); const source = eligible.find((item) => item.proforma_invoice_line_id === data.get("pi_line")); if (!source) return; const railway = project.business_scope === "RAILWAY"; await createInvoice(token, { invoice_date: data.get("date"), project_id: project.id, loa_id: data.get("loa") || undefined, customer_party_id: project.customer_party_id, business_scope: project.business_scope, railway_division_id: railway ? data.get("division") : undefined, railway_authority_id: railway ? data.get("authority") : undefined, bill_to_railway_address_id: railway ? data.get("bill_to_railway") : undefined, ship_to_railway_address_id: railway ? data.get("ship_to_railway") : undefined, bill_to_party_address_id: railway ? undefined : data.get("bill_to_party"), ship_to_party_address_id: railway ? undefined : data.get("ship_to_party"), organization_id: data.get("organization"), gst_registration_id: data.get("gst"), bank_account_id: data.get("bank"), payment_term_id: data.get("payment") || undefined, terms_version_id: data.get("terms") || undefined, tax_mode: data.get("tax_mode") || undefined, place_of_supply_state: data.get("pos_state") || undefined, place_of_supply_state_code: data.get("pos_code") || undefined, lines: [{ proforma_invoice_line_id: source.proforma_invoice_line_id, invoiced_quantity: data.get("quantity") }] }); form.reset(); setProjectId(""); await load(); }
  const customerAddresses = (masters["party-addresses"] ?? []).filter((item) => item.data.party_id === project?.customer_party_id); return <section className="space-y-8"><div><p className="text-sm tracking-[.2em] text-cyan-400">PHASE 09</p><h1 className="text-3xl font-semibold">Tax Invoices & Billing</h1><p>Create customer invoices from approved PI quantities. Procurement costs are excluded.</p></div><form onSubmit={create} className="grid gap-2 rounded border p-4 md:grid-cols-3"><h2 className="text-xl md:col-span-3">Create Tax Invoice</h2><select aria-label="Project" value={projectId} onChange={(event) => setProjectId(event.target.value)} required><option value="">Select project</option>{projects.map((item) => <option key={item.id} value={item.id}>{item.code} — {item.name}</option>)}</select><select name="loa" aria-label="LOA"><option value="">No LOA</option>{loas.filter((item) => item.project_id === projectId).map((item) => <option key={item.id} value={item.id}>{item.loa_number}</option>)}</select><input name="date" aria-label="Invoice date" type="date" required /><select name="pi_line" aria-label="Invoiceable PI line" required><option value="">Select PI material</option>{eligible.filter((item) => Number(item.remaining_invoiceable_quantity) > 0).map((item) => <option key={item.proforma_invoice_line_id} value={item.proforma_invoice_line_id}>{item.pi_number} — {item.description} — remaining {item.remaining_invoiceable_quantity} {item.unit}</option>)}</select><input name="quantity" type="number" step="0.0001" min="0.0001" placeholder="Invoice quantity" required /><select name="organization" aria-label="Organization" required><option value="">Select organization</option>{(masters.organizations ?? []).map((item) => <option key={item.id} value={item.id}>{label(item)}</option>)}</select><select name="gst" aria-label="GST registration" required><option value="">Select GST registration</option>{(masters["gst-registrations"] ?? []).filter((item) => Boolean(item.data.organization_id)).map((item) => <option key={item.id} value={item.id}>{label(item)} — {String(item.data.gstin)}</option>)}</select><select name="bank" aria-label="Bank account" required><option value="">Select bank</option>{(masters["bank-accounts"] ?? []).map((item) => <option key={item.id} value={item.id}>{label(item)}</option>)}</select><select name="payment" aria-label="Payment terms"><option value="">No payment terms</option>{(masters["payment-terms"] ?? []).map((item) => <option key={item.id} value={item.id}>{label(item)}</option>)}</select><select name="terms" aria-label="Terms version"><option value="">No T&amp;C</option>{versions.map((item) => <option key={item.id} value={item.id}>Version {item.version}</option>)}</select>{project?.business_scope === "RAILWAY" ? <><select name="division" aria-label="Railway division" required><option value="">Select division</option>{(masters["railway-divisions"] ?? []).map((item) => <option key={item.id} value={item.id}>{label(item)}</option>)}</select><select name="authority" aria-label="Railway authority" required><option value="">Select authority</option>{(masters["railway-authorities"] ?? []).map((item) => <option key={item.id} value={item.id}>{label(item)}</option>)}</select><select name="bill_to_railway" aria-label="Railway bill-to" required><option value="">Select bill-to</option>{(masters["railway-authority-addresses"] ?? []).map((item) => <option key={item.id} value={item.id}>{label(item)}</option>)}</select><select name="ship_to_railway" aria-label="Railway ship-to" required><option value="">Select ship-to</option>{(masters["railway-authority-addresses"] ?? []).map((item) => <option key={item.id} value={item.id}>{label(item)}</option>)}</select></> : <><select name="bill_to_party" aria-label="Customer bill-to" required><option value="">Select bill-to</option>{customerAddresses.map((item) => <option key={item.id} value={item.id}>{label(item)}</option>)}</select><select name="ship_to_party" aria-label="Customer ship-to" required><option value="">Select ship-to</option>{customerAddresses.map((item) => <option key={item.id} value={item.id}>{label(item)}</option>)}</select></>}<select name="tax_mode" aria-label="Tax mode"><option value="">Automatic from GST/place of supply</option><option value="INTRA_STATE">Intra-state</option><option value="INTER_STATE">Inter-state</option></select><input name="pos_state" placeholder="Place of supply state (if missing)" /><input name="pos_code" placeholder="State code" maxLength={2} /><button>Create draft Invoice</button></form><div><h2 className="text-xl">Tax Invoice register</h2>{invoices.map((item) => <article key={item.id}><Link to={`/tax-invoices/${item.id}`}>{item.invoice_number}</Link> — {item.customer_snapshot.legal_name} — ₹{item.grand_total} — {item.status}</article>)}</div></section>; }
